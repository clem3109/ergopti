# modules/

This directory is reserved for Linux-specific helper modules shared across
adapters (e.g. a D-Bus connection pool, a uinput device manager, an X11
display handle singleton).

It is intentionally empty at the scaffold stage. Populate it as adapters grow
past their TODO stubs and begin to share infrastructure.

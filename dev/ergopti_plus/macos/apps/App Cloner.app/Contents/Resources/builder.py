#!/usr/bin/env python3
# (Kept for reference) Python/Tk UI previously used but unstable on some macOS.
# This file is copied for completeness; current builder uses AppleScript.


def main():
    print(
        "This Python/Tk builder is kept for reference and is not used by default."
    )


if __name__ == "__main__":
    main()
